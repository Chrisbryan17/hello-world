# Native FreeCAD model contract

The `jcc.freecad_adapter` module creates native FreeCAD major-body scaffolds from the pure-Python engineering kernel. The generated files contain the rigid underframe, roof, two long-wall envelopes, door end, and solid end. They are geometry artifacts, not certification evidence.
